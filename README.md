Rust File-Backed Key-Value Store

A simple append-only key-value store written in Rust.
The store writes all key-value pairs to a file in a custom binary format and keeps an in-memory hash map index for fast lookups.
